
export const Pills = ({tecs}) => {
  return (
    <main>
        <ul>
            <li>
                <i className="fa-brands fa-linkedin"></i>
                <span>Linkedin</span>
            </li>
        </ul>
    </main>
  )
}


{/* <main className="products">
<ul>
    {products.slice(0,10).map(product=>(
    <li key={product.id}> 
    <img src={product.thumbnail} alt={product.titles} />
    <div>
        <strong>{product.title}</strong> - ${product.price}
    <div>
        <button>
            <AddToCartIcon/>
        </button>
    </div>
    </div>
    </li>
    ))}
</ul>
</main> */}